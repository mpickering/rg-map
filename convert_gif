#! /usr/bin/env nix-shell
#! nix-shell -i bash -p imagemagick

echo "Starting convert_gif"

if [[ $# -eq 0 ]] ; then
    echo 'Must pass output directory'
    exit 1
fi

mogrify -format jpg "$1/*.gif"
#rm "$1/*.gif"

