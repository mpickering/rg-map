#! /usr/bin/env nix-shell
#! nix-shell -i bash -p gdal
#
if [[ $# -eq 0 ]] ; then
    echo 'Must pass output directory'
    exit 1
fi

for i in $(ls "$1/groups"/*.vrt); do
	gdal2tiles.py -v -e -z 1-$2 "$i" "$1/tiles" || break
done
