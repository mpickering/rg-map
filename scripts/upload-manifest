#! /usr/bin/env nix-shell
#! nix-shell -i bash -p google-cloud-sdk

if [[ $# -eq 0 ]] ; then
    echo 'Must pass output directory'
    exit 1
fi

echo $1

gsutil cp $1 gs://rg-maps-raw -a public-read
